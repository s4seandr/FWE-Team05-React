import Navigation from "./Navigation";

const Header = () => {
    return (
        <header className="bg-dark text-white">
            <Navigation />
        </header>
    );
};

export default Header;
